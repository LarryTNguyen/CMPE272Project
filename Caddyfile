{
    admin off
    persist_config off
}

:{$PORT} {
    # Railway terminates TLS; serve plain HTTP in-container
    auto_https off

    root * dist
    encode gzip
    file_server
    try_files {path} /index.html

    log {
        output stdout
        format json
    }
}
